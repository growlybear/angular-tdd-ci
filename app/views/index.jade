extends layout

block content
  h2 Welcome to #{title}

  div(ng-controller='JobListCtrl')
    .list-group
      a.list-group-item.ng-cloak(href='#')(ng-repeat='job in jobs')(ng-cloak)
        h4.list-group-item-heading {{ job.title }}
        p.list-group-item-text {{ job.description }}
